machine:
  services:
    - docker
  node:
    version: 5.1.0

dependencies:
  post:
    - docker pull mbed/yotta
    - npm install ARMmbed/yotta-target-tests

test:
  override:    
    - node node_modules/yotta-target-tests

general:
  artifacts:
    - test_results.json